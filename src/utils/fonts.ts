import { Inter } from "next/font/google"

export const inter = Inter({
    display: "swap",
    preload: true,
    subsets: ["latin"],
    variable: "--font-inter",
    weight: ["400", "500", "600", "700"],
})
